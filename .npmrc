# This file is used by Vercel to prevent workspace detection
# For local development, workspaces are still used via root package.json
# Vercel will use rootDirectory: "frontend" which should ignore this, but this is a backup

